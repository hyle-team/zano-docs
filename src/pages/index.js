import Layout from "@theme/Layout";
import StartPage from "./StartPage/StartPage";

const Home = () => {
  return (
    <Layout title="" description="Blockchain privacy for mass adoption">
      <StartPage />
    </Layout>
  );
};

export default Home;
